const outMinMax = getElementById("minMax");
const outVett = getElementById("Vett");
const outVett50 = getElementById("Vett50");

let v = []
for(let i = 0; i < 100; i++) {
  v[i] = Math.floor(Mat.random() * 200) - 100;
}

